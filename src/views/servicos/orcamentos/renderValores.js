import { toReal } from "utils/string-utils";

const RenderValores = (values)=>{
    if(!values){ return  false; }
    //Regras de calculo
    return(
        <>
            //Alterado
        </>
    )
}

export default RenderValores;